# 文件解析任务进度

更新时间：2026-04-21  
范围：`台账分析/用户手工上传` 对应的上传文件解析任务（`FileCategoryEnum.manualUpload = true`）

## 总览

- 总任务数：11
- 已完成可解析：9
- 已创建解析类但仍占位：1
- 未创建解析类：1

## 明细进度

| 文档 | FileCategoryEnum | 解析类 | 状态 | 说明 |
| --- | --- | --- | --- | --- |
| BS.md | `BS` | `BsSheetParser` | 已完成可解析 | 使用 EasyExcel 按 DTO 读取（不做业务行过滤） |
| BS附表.md | `BS_APPENDIX_TAX_PAYABLE` | `BsAppendixSheetParser` | 已完成可解析 | 使用 EasyExcel 按 DTO 读取（不做业务行过滤） |
| PL.md | `PL` | `PlSheetParser` | 已完成可解析 | 使用 EasyExcel 按 DTO 读取 |
| PL附表-项目公司.md | `PL_APPENDIX_PROJECT` | `PlAppendixProjectCompanySheetParser` | 已完成可解析 | 使用 EasyExcel 按 DTO 读取（不做业务行过滤） |
| 印花税明细-2320、2355.md | `STAMP_TAX` | `StampTaxSheetParser` | 已完成可解析 | 使用 EasyExcel 按 DTO 读取（不做业务行过滤） |
| 增值税销项.md | `VAT_OUTPUT` | `VatOutputSheetParser` | 已完成可解析 | 双 section 动态定位 + 表头映射（不剔除业务数据行） |
| 增值税进项认证清单.md | `VAT_INPUT_CERT` | `VatInputCertificationSheetParser` | 已完成可解析 | 使用 EasyExcel 第3行表头读取（不做业务行过滤） |
| 增值税变动表附表.md | `VAT_CHANGE_APPENDIX` | `VatChangeAppendixSheetParser` | 已完成可解析 | 扫描整表定位“本期抵减预缴”并读取同一行右侧首个金额 |
| 合同印花税明细台账.md | `CONTRACT_STAMP_DUTY_LEDGER` | `ContractStampDutyLedgerSheetParser` | 已完成可解析 | 使用 EasyExcel 按 DTO 读取（不做业务行过滤） |
| 睿景景程月结数据表-报税.md | `MONTHLY_SETTLEMENT_TAX` | `MonthlySettlementTaxSheetParser` | 占位待实现 | 当前返回 `parser is initialized only` |
| 累计项目税收明细表.md | `CUMULATIVE_PROJECT_TAX` | - | 未开始 | 尚未创建对应 `SheetParser` 类 |

## 下一步建议（按优先级）

1. 完成 `MonthlySettlementTaxSheetParser`（2320/2355 相关计算链路会用到）。
2. 新建并实现 `CUMULATIVE_PROJECT_TAX` 对应解析器（当前完全缺失）。
