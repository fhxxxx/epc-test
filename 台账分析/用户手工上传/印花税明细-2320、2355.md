# 台账 Sheet 分析报告 - 印花税明细-2320、2355

适用文件：`税务台账生成逻辑_20260415V5.0(1).xlsx`  
适用范围：单 sheet（`印花税明细-2320、2355`）

---

## 1. 文档头信息

| 项目 | 内容 |
|---|---|
| Sheet名称 | 印花税明细-2320、2355 |
| Sheet编号 | 22 |
| 颜色标签 | 标蓝 |
| 功能标签 | 用户手工上传 |
| 是否纳入台账展示 | 是 |
| 适用公司范围 | 2320、2355 |
| 版本基线 | 税务台账生成逻辑_20260415V5.0(1).xlsx |
| 分析日期 | 2026-04-16 |
| 分析人 | Codex |

---

## 2. 业务定位

- 本页为用户手工上传页。
- 你关注的是下方“汇总表”，用于按合同类别汇总印花税应税与应纳数据。
- 本页不属于自动计算页，无跨 sheet 自动数据交互机制。

---

## 3. 汇总表表头（按图片）

汇总表有效字段：
- 合同类别
- 应税金额
- 税率
- 应纳税额

> “汇总”是区块标签，不作为业务字段。

---

## 4. Java 对象（按汇总表）

```java
package com.envision.epc.module.taxledger.dto;

import lombok.Data;
import java.math.BigDecimal;

@Data
public class StampDutySummaryRowDTO {

    /** 合同类别 */
    private String contractCategory;

    /** 应税金额 */
    private BigDecimal taxableAmount;

    /** 税率 */
    private BigDecimal taxRate;

    /** 应纳税额 */
    private BigDecimal taxPayableAmount;
}
```

---

## 5. JSON 结构化表达（jsonArray）

```json
[
  {
    "contractCategory": "租赁合同-租车",
    "taxableAmount": 461162.19,
    "taxRate": 0.001,
    "taxPayableAmount": 461.16
  },
  {
    "contractCategory": "建设工程合同-施工",
    "taxableAmount": 1559220358.92,
    "taxRate": 0.0003,
    "taxPayableAmount": 467766.11
  }
]
```

---

## 6. 结论

- 该页应按“标蓝 + 用户手工上传”管理。
- 解析下方汇总表时，建议直接按“合同类别、应税金额、税率、应纳税额”四列映射。

---

## 7. 当前解析实现口径（与代码同步）

- 解析器：`StampTaxSheetParser`（`FileCategoryEnum.STAMP_TAX`）。
- 读取范围：只读取“汇总”标签对应区块，不读取上方“采购合同/销售合同”等其他区块。
- 表头识别：识别同时包含 `合同类别/应税金额/税率/应纳税额` 的表头行，且该行或上一行包含“汇总”标识。
- 数据终止条件：从汇总表头下一行开始读取，遇到空行或下一个表头即停止。
- 空行过滤：四字段均为空（或等价空值）的行不输出。
- 数值字段默认：`taxableAmount/taxRate/taxPayableAmount` 解析为空时，统一补 `0`。
