# 台账 Sheet 分析报告 - 增值税变动表附表

适用文件：`税务台账生成逻辑_20260415V5.0(1).xlsx`  
适用范围：单 sheet（`增值税变动表附表`）

---

## 1. 文档头信息

| 项目 | 内容 |
|---|---|
| Sheet名称 | 增值税变动表附表 |
| Sheet编号 | 16 |
| 颜色标签 | 标蓝 |
| 功能标签 | 用户手工上传 |
| 是否纳入台账展示 | 是 |
| 适用公司范围 | all |
| 版本基线 | 税务台账生成逻辑_20260415V5.0(1).xlsx |
| 分析日期 | 2026-04-16 |
| 分析人 | Codex |

---

## 2. 业务定位

- 本页是用户手工上传页，用于补充增值税变动相关明细。
- 本页不属于自动计算页，无跨 sheet 自动数据交互机制。

---

## 3. 关键属性标记（必须提取）

### 3.1 关键属性
- `本期抵减预缴`

### 3.2 提取规则
- 在页面底部汇总区定位标签文本：`本期抵减预缴`。
- 读取该标签同一行右侧金额单元格作为属性值。
- 入库时按金额数值解析（忽略千分位展示格式）。

### 3.3 建议字段名
- `currentPeriodPrepaidDeduction`：本期抵减预缴

---

## 4. 数据交互机制说明

- 本页为“纯上传输入”，不从其他 sheet 自动取数。
- 本页数据由用户维护上传，系统按结构读取。
- 提取出的 `currentPeriodPrepaidDeduction` 供下游增值税变动逻辑使用。

---

## 5. 结论

- 该页应按“标蓝 + 用户手工上传”管理。
- 原文中“计算链路、跨表依赖、空白占位”相关内容不适用，已删除。

---

## 6. JSON结构化表达

```json
[
  {
    "currentPeriodPrepaidDeduction": 97523.50
  }
]
```
