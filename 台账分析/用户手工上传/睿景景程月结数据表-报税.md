# 台账 Sheet 分析报告 - 睿景景程月结数据表-报税

适用文件：`税务台账生成逻辑_20260415V5.0(1).xlsx`  
适用范围：单 sheet（`睿景景程月结数据表-报税`）

---

## 1. 文档头信息

| 项目       | 内容                            |
| -------- | ----------------------------- |
| Sheet名称  | 睿景景程月结数据表-报税                  |
| Sheet编号  | 23                            |
| 颜色标签     | 标蓝                            |
| 功能标签     | 用户手工上传                        |
| 是否纳入台账展示 | 是                             |
| 适用公司范围   | 2320，2355                     |
| 版本基线     | 税务台账生成逻辑_20260415V5.0(1).xlsx |
| 分析日期     | 2026-04-17                    |
| 分析人      | Codex                         |

---

## 2. 业务定位

- 本页为用户手工上传文件页。
- 本页不属于自动计算页，无跨 sheet 自动数据交互机制。
- 按你提供图片，本次以该区块表头进行对象建模。

---

## 3. 表头口径（按图片）

- 第一行字段（title）：`建安9%`（示例，按实际首行标题取值）
- 第二行表头字段：
  - 成本
  - 收入
  - 销项
  - 已开票收入
  - 已开票税额
  - 未开票收入
  - 未开票税额

---

## 4. Java 对象（首行title作为字段）

```java
package com.envision.epc.module.taxledger.dto;

import lombok.Data;
import java.math.BigDecimal;

@Data
public class MonthlyTaxSectionDTO {

    /** 第一行标题（例如：建安9%） */
    private String title;

    /** 成本 */
    private BigDecimal cost;

    /** 收入 */
    private BigDecimal income;

    /** 销项 */
    private BigDecimal outputTax;

    /** 已开票收入 */
    private BigDecimal invoicedIncome;

    /** 已开票税额 */
    private BigDecimal invoicedTaxAmount;

    /** 未开票收入 */
    private BigDecimal uninvoicedIncome;

    /** 未开票税额 */
    private BigDecimal uninvoicedTaxAmount;
}
```

---

## 5. JSON 结构化表达（jsonArray）

```json
[
  {
    "title": "建安9%",
    "cost": 0.00,
    "income": 0.00,
    "outputTax": 0.00,
    "invoicedIncome": 0.00,
    "invoicedTaxAmount": 0.00,
    "uninvoicedIncome": 0.00,
    "uninvoicedTaxAmount": 0.00
  }
]
```

---

## 6. 结论

- 该页应按“标蓝 + 用户手工上传”管理。
- 解析时需先取首行 `title`，再取第二行字段对应数值。
