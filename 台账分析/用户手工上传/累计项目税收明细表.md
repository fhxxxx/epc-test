# 台账 Sheet 分析报告 - 累计项目税收明细表

适用文件：`税务台账生成逻辑_20260415V5.0(1).xlsx`  
适用范围：单 sheet（`累计项目税收明细表202512`）

---

## 1. 文档头信息

| 项目       | 内容                            |
| -------- | ----------------------------- |
| Sheet名称  | 累计项目税收明细表                     |
| Sheet编号  | 15                            |
| 颜色标签     | 标蓝                            |
| 功能标签     | 用户手工上传                        |
| 是否纳入台账展示 | 是                             |
| 适用公司范围   | 2320、2355                     |
| 版本基线     | 税务台账生成逻辑_20260415V5.0(1).xlsx |
| 分析日期     | 2026-04-16                    |
| 分析人      | Codex                         |

---

## 2. 业务定位

- 本页是累计项目税收明细的上传承载页。
- 数据来源为用户手工上传，不通过系统内规则自动计算生成。

---

## 3. 数据交互机制说明

- 本页属于“纯上传输入页”，无跨 sheet 自动取数逻辑。
- 本页不依赖其他 sheet 作为计算输入。
- 本页不承担计算中间过程，不设置数据回写交互机制。

---

## 4. 结论

- 该页应按“标蓝 + 用户手工上传”管理。
- 原先文档中的“计算页、跨表交互、计算链路”等内容不适用于本页，已删除。

---

## 5. Parser处理策略

- `FileCategoryEnum.CUMULATIVE_PROJECT_TAX` 采用 No-Op Parser。
- 行为定义：
  - 文件允许上传并入库；
  - 异步解析阶段返回空结果（`data=null`）；
  - 不产出 `issues`，解析状态记为成功；
  - 当前阶段不做结构化字段抽取。
- 设计目的：
  - 保留上传与文件管理能力；
  - 避免落入默认 `UnsupportedSheetParser` 导致 `parse_status=FAILED`；
  - 为后续如需启用正式解析预留扩展点。
